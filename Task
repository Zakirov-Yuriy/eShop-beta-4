<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Title</title>
</head>
<body>
<p>Open console</p>
<script>

  // Дан большой текст, в котором для оформления прямой речи используются одинарные кавычки. Придумать шаблон, который заменяет одинарные кавычки на двойные.

  console.log('-Task 1 -');

  let text1 = "'Мы уезжаем завтра утром', — сказал отец 'Мы уезжаем завтра утром', — сказал отец 'Мы уезжаем завтра утром', — сказал отец 'Мы уезжаем завтра утром', — сказал отец 'Мы уезжаем завтра утром', — сказал отец 'Мы уезжаем завтра утром', — сказал отец 'Мы уезжаем завтра утром', — сказал отец 'Мы уезжаем завтра утром', — сказал отец";
  console.log('Before: ' + text1);

  console.log('After:  ' + text1.replace(/'/g, '"'));


  // Улучшить шаблон так, чтобы в конструкциях типа aren't одинарная кавычка не заменялась на двойную.

  console.log('- Task 2 -');

  let text2 = "'We are leaving tomorrow morning', said the father, they can't gotcha us";
  console.log('Before: ' + text2);

  console.log('After:  ' + text2.replace(/\B'|'\B/g, '"'));
</script>
</body>
</html>